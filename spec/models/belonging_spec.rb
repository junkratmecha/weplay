require 'rails_helper'
describe Belonging do

  # context 'create sucess case' do
    # it "can create belonging" do
    #   user = build(:user)
    #   clan = build(:clan)
    #   belonging = build(:belonging, user: user, clan: clan)
    #   expect(belonging).to be_valid
    # end
  # end
end